<UserControl x:Class="Modinator.Views.HeroViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="2" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Mode accent strip -->
        <Border Grid.Row="0" Background="{StaticResource AccentBrush}" />

        <!-- ═══════════════════════════════════════════════════════════
             HERO VIEWER — Command Bar
             ═══════════════════════════════════════════════════════════ -->
        <Border Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="16,10">
            <StackPanel>
                <DockPanel LastChildFill="False">
                    <StackPanel Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Border Background="{StaticResource AccentSubtleBrush}"
                                BorderBrush="{StaticResource AccentBrush}" BorderThickness="1"
                                CornerRadius="4" Padding="8,4"
                                VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE77B;" FontFamily="Segoe MDL2 Assets"
                                           FontSize="11" Foreground="{StaticResource AccentBrush}"
                                           Margin="0,0,6,0" VerticalAlignment="Center" />
                                <TextBlock Text="HERO VIEWER"
                                           Foreground="{StaticResource AccentBrush}"
                                           FontSize="10.5" FontWeight="Bold"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource SurfaceLightBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="5"
                                Padding="10,5"
                                Margin="10,0,0,0"
                                VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE946;"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="11"
                                           Foreground="{StaticResource TextMutedBrush}"
                                           Margin="0,0,7,0"
                                           VerticalAlignment="Center" />
                                <TextBlock x:Name="LblStatus"
                                           Text="Click SCAN HEROES to list your hero deck."
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="11.5"
                                           VerticalAlignment="Center"
                                           MaxWidth="420"
                                           TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Right"
                                Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="16,0,0,0">
                        <Button x:Name="BtnScan"
                                Style="{StaticResource AccentButton}"
                                Padding="18,7" Height="32"
                                ToolTip="List your heroes with their stats and equipped items."
                                Click="BtnScan_Click">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE71E;" FontFamily="Segoe MDL2 Assets"
                                           FontSize="12" VerticalAlignment="Center" Margin="0,0,7,0" />
                                <TextBlock Text="SCAN HEROES" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </DockPanel>

                <TextBlock Text="Double-click a hero to edit it  ·  double-click an equipped item to open it in the item editor"
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="10" Opacity="0.65"
                           Margin="0,10,0,0" />
            </StackPanel>
        </Border>

        <!-- ═══════════════════════════════════════════════════════════
             Hero cards
             ═══════════════════════════════════════════════════════════ -->
        <Grid Grid.Row="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Padding="12">
                <WrapPanel x:Name="CardPanel"
                           Margin="6" />
            </ScrollViewer>

            <!-- Empty / first-run placeholder -->
            <StackPanel x:Name="EmptyState"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        IsHitTestVisible="False">
                <TextBlock x:Name="EmptyIcon" Text="&#xE77B;"
                           FontFamily="Segoe MDL2 Assets"
                           FontSize="44"
                           Foreground="{StaticResource TextMutedBrush}"
                           Opacity="0.45"
                           HorizontalAlignment="Center" />
                <TextBlock x:Name="EmptyText"
                           Text="Click SCAN HEROES to list your hero deck."
                           Foreground="{StaticResource TextMutedBrush}"
                           FontSize="12.5" Opacity="0.8"
                           Margin="0,12,0,0"
                           HorizontalAlignment="Center" TextAlignment="Center" />
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
