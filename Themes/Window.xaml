<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Modinator.Themes">

    <!-- ================================================================
         Custom Window Chrome Style
         Modern dark title bar with min/max/close buttons.
         WindowCommandHelper attached behavior auto-wires SystemCommands.
         ================================================================ -->

    <Style x:Key="ModernWindowStyle" TargetType="Window">
        <Setter Property="Background" Value="{StaticResource BackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource DefaultFontFamily}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="local:WindowCommandHelper.Enabled" Value="True" />
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome GlassFrameThickness="0"
                              CornerRadius="8"
                              CaptionHeight="32"
                              ResizeBorderThickness="5"
                              UseAeroCaptionButtons="False" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Window">
                    <Border x:Name="WindowBorder"
                            Background="{StaticResource BackgroundBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="32" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!-- Title Bar -->
                            <Grid Grid.Row="0"
                                  Background="{StaticResource TitleBarBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- App Icon -->
                                <Image Grid.Column="0"
                                       Source="{TemplateBinding Icon}"
                                       Width="16" Height="16"
                                       Margin="12,0,8,0"
                                       VerticalAlignment="Center"
                                       RenderOptions.BitmapScalingMode="HighQuality" />

                                <!-- Window Title -->
                                <TextBlock Grid.Column="1"
                                           Text="{TemplateBinding Title}"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontFamily="{StaticResource DefaultFontFamily}"
                                           FontWeight="SemiBold"
                                           FontSize="12"
                                           VerticalAlignment="Center"
                                           TextTrimming="CharacterEllipsis" />

                                <!-- Window Control Buttons -->
                                <StackPanel Grid.Column="2"
                                            Orientation="Horizontal"
                                            HorizontalAlignment="Right">

                                    <!-- Per-window title bar content (e.g. quick toggles) -->
                                    <ContentPresenter
                                        Content="{Binding Path=(local:WindowExtensions.TitleBarContent), RelativeSource={RelativeSource TemplatedParent}}"
                                        VerticalAlignment="Center"
                                        Margin="0,0,6,0"
                                        WindowChrome.IsHitTestVisibleInChrome="True" />

                                    <!-- Minimize -->
                                    <Button x:Name="MinimizeButton"
                                            Style="{StaticResource TitleBarButton}"
                                            Command="{x:Static SystemCommands.MinimizeWindowCommand}">
                                        <TextBlock Text="&#x2501;"
                                                   FontSize="10"
                                                   Foreground="{StaticResource TextSecondaryBrush}" />
                                    </Button>

                                    <!-- Maximize/Restore -->
                                    <Button x:Name="MaximizeButton"
                                            Style="{StaticResource TitleBarButton}"
                                            Command="{x:Static SystemCommands.MaximizeWindowCommand}">
                                        <TextBlock x:Name="MaximizeIcon"
                                                   Text="&#x25A1;"
                                                   FontSize="12"
                                                   Foreground="{StaticResource TextSecondaryBrush}" />
                                    </Button>

                                    <!-- Close -->
                                    <Button x:Name="CloseButton"
                                            Style="{StaticResource TitleBarCloseButton}"
                                            Command="{x:Static SystemCommands.CloseWindowCommand}">
                                        <TextBlock Text="&#x2715;"
                                                   FontSize="10"
                                                   Foreground="{StaticResource TextSecondaryBrush}" />
                                    </Button>
                                </StackPanel>
                            </Grid>

                            <!-- Main Content Area -->
                            <AdornerDecorator Grid.Row="1">
                                <ContentPresenter ClipToBounds="True" />
                            </AdornerDecorator>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="WindowBorder" Property="CornerRadius" Value="0" />
                            <Setter TargetName="WindowBorder" Property="BorderThickness" Value="0" />
                            <Setter TargetName="WindowBorder" Property="Margin" Value="7" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
